# 裸号存活实验(进行中)

## 目的
分离"协议注册账号快速被封"的两个候选死因:
- **解释 A**:OpenAI 现在清所有无手机号(→ 接码必须前置,`ENABLE_CODEX_AUTO=True`)
- **解释 B**:清理目标是"注册会话机器人评分高"的号(缺 jsd 质询/遥测)(→ 复刻 jsd/遥测后,延迟接码策略仍可用)

## 实验账号
- 邮箱:`homfwkl09081@outlook.com`(outlook 池,Graph 可收码)
- 注册方式:**真 Edge 浏览器手动注册**(capture-exp.mjs / profile-exp),注册后发送过 1 条消息
- 注册时间:2026-09-18 09:00(本地)
- 关键变量:**无手机、无 Codex 授权**(严格裸号)
- 代理:与协议注册同款 clash 出口(SJC),保持环境一致

## 实验时间线
- 2026-09-18 09:00 注册完成(邮箱验证→资料→登录→1 条对话)
- 2026-09-18 09:02 基线查活(协议):未跑通(出口节点 CF 质询,网络层问题,与账号无关)
- 2026-09-18 09:15 协议登录链验证:authorize → OTP validate 全部通过(账号功能正常),
  仅 callback 文档导航被节点质询,token 未取到
- 2026-09-18 09:18 **基线确立:浏览器会话 backend-api 认证接口 200 = 账号存活(T+18min)**
- 2026-09-18 09:50 **T+50min 仍存活**(sentinel/heartbeat 200 持续)
- 后续:按用户指令间隔查活(预计 1-2 小时一次),结果追加到这里

## 对照组数据(协议注册,dzgolpes248504@outlook.com)
- 2026-09-18 09:24 协议注册完成(拿到 accessToken)
- 2026-09-18 09:47 查活:reauth OTP 提交后 email-otp/validate 返回 403+account_deactivated
- **T+23min 死亡**(同一小时、同一出口、同一邮箱池)

## 追加数据(2026-09-18 14:30)
- Cloak 有头注册 dzsyjsel89912@outlook.com:14:28 提交注册,14:30 OTP 验证即返回
  account_deactivated(**T+约2分钟**)。流程机械部分全部正常
  (结构定位/提交/服务端响应),是 OpenAI 风控直接拒号。
- 死亡速度曲线:6h → 5.5h → 23min → ~2min。同一出口 IP + 同一邮箱池 +
  同一注册模式的簇风险分随每次注册快速累积,新号秒进黑名单。
- 结论固化:**当前"出口 IP × 邮箱池"组合已被 OpenAI 风控标记,注册通道代码
  已不是瓶颈**。恢复注册能力需要:更换干净住宅 IP + 全新邮箱源 + 大幅降低
  单 IP 注册频率(1-2 个/天),并把注册行为(停留/互动)做得更接近真人。

## 阶段性结论(2026-09-18 09:50)
同小时同 IP 同邮箱池的对照:浏览器注册 T+50min 活,协议注册 T+23min 死。
→ **死因锁定注册会话本身被 OpenAI/CF 标记**(协议会话无 jsd 质询、无 RUM/CES 遥测、
curl_cffi TLS),而非缺手机(手机是次级信任层)。
→ 死亡速度递增(6h→5.5h→23min)= 同出口/同画像簇的风险分累积,注册越多死越快。
→ 延迟接码策略对协议号不可行;自救方向 = 把协议注册会话的机器人信号洗掉
(jsd 复刻 > RUM/CES 遥测 > FlareSolverr clearance)。

## 账号管理状态
实验号暂不在账号管理库(手动注册,无 accessToken)。第一次查活 `live` 时会用
返回的 accessToken 执行 save_account_data 正式入库;若 `deactivated` 则仅记录结论。

## 结论判定(查活 status)
- `live` → 解释 B 成立:死因是协议注册会话被标记。行动:复刻 jsd 质询 + RUM/CES 遥测进协议链路;
  账号本身用 save_account_data 入库账号管理。
- `deactivated` → 解释 A 成立:无手机号必清。行动:开启 `ENABLE_CODEX_AUTO=True`,接码前置到注册时。
- `failed`(网络) → 节点问题,冷却后重跑,不计结论。

## 参考样本
- 手动号(活):homvlc432405@outlook.com —— 注册时完成手机验证 + Codex 授权
- 协议号(死):hofwekv35395@outlook.com(6h)、hollvz35660@outlook.com(5.5h)—— 均无手机无授权
- 抓包:reg-capture/capture-pw.*(手动完整流程)、capture-exp.*(本实验裸注册,含真浏览器 jsd/RUM 交互,可作复刻蓝本)
